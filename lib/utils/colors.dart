import 'package:flutter/material.dart';

class ColorPicker {
  static const backgroundColor = Colors.white;
  static const primaryColor = Color.fromARGB(255, 2, 231, 10);
  static const primaryDarkColor = Color.fromARGB(255, 1, 92, 4);
}
